% Sprawdzenie wyniku unifikacji dwóch stałych
A=B.

% Sprawdzenie wyniku unifikacji stałej ze zmienną
A=b.

% Sprawdzenie wyniku unifikacji dwóch zmiennych
a=b.

% Sprawdzenie czy A jest zmienną
var(A).

% Sprawdzenie czy A nie jest zmienną
nonvar(A).

% Sprawdzenie czy A jest stałą ale nie zmienną
atom(A).

% Sprawdzenie czy A jest liczbą
number(A).

% Sprawdzenie czy A jest termem złożonym
compound(A).
